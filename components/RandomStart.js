import React, { Component } from 'react';
import { Text, View, Button, Switch } from 'react-native';
import { withNavigation } from 'react-navigation';

export default RandomStart = (props) => {
  return (
    <View>
      <Text>
 
      </Text>
    </View>
  )
}